apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: my-container
      image: nginx
      env:                  # ✅ correct field
        - name: ENV
          value: dev
        - name: APP_NAME
          value: dockersiva
